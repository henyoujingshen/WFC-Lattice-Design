# 网页可视化源码

## 入口文件

`app.py`

## 主要内容

- `app.py`：Gradio 网页可视化主入口。
- `wfc_toolbox`：Gaussian field、WFC、点阵可视化和 STL 导出相关模块。
- `requirements.txt`：源码运行依赖。
- `run.bat`、`install_deps.bat`：Windows 下的运行和依赖安装脚本。

运行方式：双击 `run.bat`，或执行 `python app.py`。
